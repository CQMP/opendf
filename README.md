open-df - a code to evaluate DF diagrams 

To compile - use standard `cmake -DCMAKE_BUILD_TYPE=Release ${PATH_TO_CODE}`.

Dependencies:
 - boost
 - eigen3 
 - fftw
 - gftools (header-only; fetched automatically during build)
